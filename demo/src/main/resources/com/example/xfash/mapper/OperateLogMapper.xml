<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.xfash.Mapper.OperateLogMapper">

    <select id="list" resultType="com.example.xfash.pojo.OperateLog">
        SELECT
        operate_log.id,
        operate_emp_id as operateEmpId,
        operate_time as operateTime,
        class_name as className,
        method_name as methodName,
        method_params as methodParams,
        return_value as returnValue,
        cost_time as costTime,
        emp.name as operateEmpName
        FROM operate_log
        join emp on emp.id=operate_emp_id
        ORDER BY operate_time DESC
    </select>
</mapper>
